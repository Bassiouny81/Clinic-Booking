{
  "framework": null,
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/clinic-app build",
  "outputDirectory": "apps/clinic-app/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
